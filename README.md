# Early-Motor-Imagery-Classification
Early motor imagery (MI) classification from EEG functional-connectivity graphs using an SE-enhanced Transformer, evaluated with 8-fold cross-validation and early (≤800 ms) decoding.
